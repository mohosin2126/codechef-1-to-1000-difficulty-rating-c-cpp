#include <bits/stdc++.h>
using namespace std;

int main()
{
    int T;
    cin >> T;

    while (T--)
    {
        int N;
        cin >> N;
        int polybags = (N + 9) / 10;

        cout << polybags << endl;
    }
    return 0;
}

#include <bits/stdc++.h>
using namespace std;
int main()
{
    int T;
    cin >> T;

    while (T--)
    {
        int N;
        cin >> N;
        int squareRoot = floor(sqrt(N));
        cout << squareRoot << endl;
    }

    return 0;
}